# Copy to .env and fill in. Real secrets live only here (local) or in Vercel
# project settings — never committed, never the plaintext password.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
SESSION_SECRET=
AUTH_PASSWORD_HASH=
